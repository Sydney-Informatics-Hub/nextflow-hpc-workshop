// Local config for testing and development

// Enable use of Singularity to run containers
singularity {
    enabled = true
    autoMounts = true
    autoCleanUp = true
	cacheDir = "${projectDir}/simg"
}
